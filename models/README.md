# Formal models

- `v3-predecessor/` contains the predecessor self-logging CPN before sensitivity controls.
- `v4-sensitivity/` contains the final public model used for all v4 data.
- `aom/` contains the editable three-page AOM goal models.

Do not rename development snapshots as public versions. The manuscript and repository expose only the v3 → v4 sequence.
